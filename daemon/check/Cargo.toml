[package]
name = "omen-hub-check"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
omen-hub-fan = { path = "../crates/fan" }
omen-hub-system = { path = "../crates/system" }
# Only for power_mode_available(): the verdict this prints must match the
# daemon's, and power modes are half of it.
omen-hub-power = { path = "../crates/power" }
serde_json = "1"
